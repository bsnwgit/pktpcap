# pktPCAP Python dependencies (new FastAPI backend under app/)
# The legacy Flask app under service/ has its own service/requirements.txt
# and is left untouched until cutover.

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# App database
aiosqlite>=0.20.0

# Auth
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0               # Fernet encryption for stored credentials
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2
httpx>=0.27.0
python3-saml>=1.16.0

# Notifications
aiosmtplib>=3.0.1
jinja2>=3.1.4

# AI Assistant (pktpcap supports both providers, unlike some sibling apps)
anthropic>=0.30.0
openai>=1.35.0

# Config
pyyaml>=6.0.1
python-dotenv>=1.0.0

# Utilities
python-dateutil>=2.9.0
